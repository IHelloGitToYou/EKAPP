<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ek">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".TakeOutActivity" />
        <activity
            android:name=".JK_SOActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".SelectSOItemActivity" />
        <activity
            android:name=".HistoryJLActivity"
            android:label="@string/title_activity_history_jl"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ShowHistoryOnlyActivity"
            android:label="@string/title_activity_show_history_only"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>